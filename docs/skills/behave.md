# Behave Patterns

Canonical reference: **`docs/behave-patterns.md`**

Load `docs/behave-patterns.md` directly. It covers shared SSH helpers, smoke suite local execution model, AmbiguousStep avoidance, @future scaffolding, and suite directory layout.

